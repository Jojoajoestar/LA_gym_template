import '../styles/globals.css'; // Import the main global CSS
import '../styles/Contact.css'; // Import the Contact.css here

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
